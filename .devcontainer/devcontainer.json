{
  "name": "ShadowScan Flutter",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "ShadowScan Web Preview",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "sed -i 's/withValues(alpha: \\.15)/withOpacity(.15)/g; s/withValues(alpha: \\.25)/withOpacity(.25)/g' lib/main.dart && flutter config --enable-web && flutter pub get",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ]
    }
  },
  "remoteUser": "vscode"
}
